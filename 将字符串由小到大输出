#include<iostream>
using namespace std;
void swap(string* a, string* b)
{
	string* d;
	d =  a;
	*a = * b;
	b = d;
}
int main()
{
	string a, b, c;
	cin >> a >> b >> c;
	if (a > b)
	{
		swap(a, b);
	}
	if (a > c)
	{
		swap(a, c);
	}
	if (b > c)
	{
		swap(b, c);
	}
	cout << a << endl << b << endl<< c;
}
