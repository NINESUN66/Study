#include<iostream>
using namespace std;

int main()
{
	int arr[6],i;
	for (int a = 0; a < 6; a++)
	{
		cin >> arr[a];
	}
	for (i = 0; i < 6; i++)
	{
		if (arr[i] < arr[i + 1])
		{
			cout << "YES";
			break;
		}
		else {
			cout << "NO";
			break;
		}
	}
}
